import fs from 'node:fs/promises';
const root=new URL('../../../',import.meta.url);
const append=async(file,content)=>{const url=new URL(file,root);const current=await fs.readFile(url,'utf8');const heading=content.trim().split('\n')[0];if(!current.includes(heading))await fs.writeFile(url,current.trimEnd()+'\n\n'+content.trim()+'\n');};
await append('docs/modules/clinical_access.md',`## Corrección auditada H04/H05 — 17/09/2026

La migración 20260918020000 conserva un comprobante idempotente: repetir finalize_clinical_encounter con la misma autorización, autor, clave y contenido devuelve la atención original, incluso tras revocación, sin escribir. Reutilizar la clave con otro contenido u operación se rechaza. Preparar documentos también vincula la clave a la metadata original. Una confirmación documental ready es repetible; los pendientes revalidan permisos.

El propietario puede revocar solicitudes approved o completed; ambas pasan a revoked y se retiran los permisos residuales sin borrar atenciones. Mobile ofrece Revocar permisos pendientes sobre completed vigente. Web separa atención guardada de archivo pendiente, y conserva claves/archivo/fases en memoria durante el reintento; recargar la página pierde ese contexto, sin persistencia clínica local.

Servidor aplicado; cambios de clientes locales, publicación y QA nativo pendientes. Evidencia y límites en docs/audit/2026-09-17/CORRECCION_REINTENTOS.md. No amplía el alcance clínico de producto.`);
await append('docs/api/API_CONTRACT.md',`## Clinical Access — reintentos y retiro residual (20260918020000)

- finalize_clinical_encounter: misma firma. La clave del profesional identifica una operación de una autorización y un contenido inmutable (fecha, tipo, resumen y entradas originales normalizadas). Repetirla devuelve el UUID finalizado, sin nuevo evento; cambiar contenido u autorización se rechaza. Obtener ese comprobante propio no requiere restablecer el consentimiento retirado.
- prepare_clinical_document_upload: misma clave/autor requiere mismo encounter y metadata; devuelve la misma preparación. Los permisos vigentes siguen siendo obligatorios.
- finalize_clinical_document_upload: el autor puede repetir ready sin escrituras. Pending requiere guard vigente y objeto Storage válido. Un objeto ausente/inválido devuelve Clinical document validation failed; permanece pending.
- uploadPreparedClinicalDocument: confirma primero para recuperar respuestas perdidas; si aún falta el objeto, sube sin upsert y confirma. Un rechazo de permiso no inicia subida.
- revoke_clinical_write_authorization: acepta approved y completed, mueve solicitud a revoked, conserva historia; repetir revoked es éxito sin evento adicional. Sigue requiriendo can_edit_pet.

Sin cambios de DTOs o firmas. Servidor aplicado; clientes requieren publicación. Especificación y resultados en docs/audit/2026-09-17/CORRECCION_REINTENTOS.md.`);
await append('docs/data/RLS_RULES.md',`## Clinical Access — comprobantes y revocación completed

20260918020000 no amplía grants ni políticas de tablas. Las funciones SECURITY DEFINER conservan actor auth.uid() y search_path fijo. Un comprobante de atención requiere el autor profesional de la solicitud/autorización y la clave/contenido original; un comprobante documental ready requiere su autor. Son retornos sin mutación, admisibles después de retirar permisos. Nuevos datos y documentos pending mantienen el guard de 20260918010000.

Revocar autorización completed requiere can_edit_pet, bloquea la solicitud y marca autorización/solicitud revocadas. Conserva historial. La regresión remota comprueba que el profesional no puede revocar por ser autor; la suite local usa un stub permisivo y no acredita ese aislamiento por sí sola.`);
await append('docs/data/SUPABASE_SCHEMA.md',`## Migración 20260918020000 — recuperación clínica

clinical_retry_and_residual_revocation modifica finalize_clinical_encounter, prepare_clinical_document_upload, finalize_clinical_document_upload y revoke_clinical_write_authorization. No añade tablas/columnas. Define reintentos inmutables con claves existentes, confirmación documental ready idempotente y transición completed → revoked para retirar capacidad residual. Atenciones y documentos finalizados permanecen. Aplicada y verificada en el proyecto vinculado el 18/09/2026 UTC; evidencia en docs/audit/2026-09-17/evidence/clinical-retry-deploy.json.`);
await append('docs/ux/SCREEN_SPECIFICATIONS.md',`## Recuperación clínica y revocación residual — auditoría 17/09/2026

Web /clinical-access/[token]: al confirmar una atención se congela la operación. Si no se conoce el resultado, mostrar Confirmación pendiente / Recuperar confirmación. Si la atención está guardada pero falta el documento, mostrar Atención guardada / Reintentar documento. Si solo falta cargar el comprobante, mostrar Actualizar comprobante. Deshabilitar doble envío mientras hay operación; reintentar conserva claves, contenido y archivo en memoria. No presentar el fallo de adjunto como pérdida de toda la atención. Recargar/cerrar pierde el contexto en memoria; no hay persistencia clínica local.

Mobile Salud > Acceso clínico > Historial de autorizaciones: una solicitud completed aún vigente permite Revocar permisos pendientes; explicar que conserva la atención e impide completar documentos pendientes. Tras revocar se muestra Permiso revocado. El backend verifica can_edit_pet y el estado vigente.

Web probada localmente con subida real y respuesta perdida simulada. Pantalla nativa pendiente de QA en dispositivo y ambos clientes pendientes de publicación.`);
const reportUrl=new URL('docs/audit/2026-09-17/INFORME_AUDITORIA.md',root);
let report=await fs.readFile(reportUrl,'utf8');
report=report.replace('H04–H08 siguen pendientes. Detalle en `CORRECCION_CLINICA.md`.','H04/H05 tienen además corrección de servidor aplicada y clientes locales implementados, con publicación pendiente; H06–H08 siguen abiertos. Detalle en `CORRECCION_CLINICA.md` y `CORRECCION_REINTENTOS.md`.');
const block=`\n\n## Actualización H04/H05: servidor aplicado, clientes por publicar\n\nMigración 20260918020000 aplicada tras comparar cuatro funciones remotas y probar la candidata dentro de rollback. Pasaron 36 casos SQL locales, 9 de cliente/servicio y 25 remotos antes y después. Web local completó 7 comprobaciones con un PNG real y pérdida simulada de respuesta: recuperó el documento sin duplicar atención ni subida. El owner revocó la autorización completed y mantuvo el historial.\n\nEl botón mobile de revocación residual está implementado, sin prueba en dispositivo. No hubo despliegue web ni distribución mobile. La recuperación web conserva claves/archivo en memoria y se pierde al recargar. Las pruebas SQL no simulan dos conexiones concurrentes. Perfil QA suspendido nuevamente y accesos revocados. Ver CORRECCION_REINTENTOS.md.\n`;
if(!report.includes('## Actualización H04/H05:'))report=report.replace('## Resultado principal',block+'\n## Resultado principal');
await fs.writeFile(reportUrl,report);
const previous=new URL('docs/audit/2026-09-17/CORRECCION_CLINICA.md',root);
let old=await fs.readFile(previous,'utf8');old=old.replace('H04 y H05 continúan abiertos: idempotencia/recuperación de atención y permisos residuales de adjuntos tras estado completed.','Actualización posterior: H04/H05 tienen servidor corregido y clientes locales implementados; publicación y QA nativo pendientes. Ver CORRECCION_REINTENTOS.md para resultados y límites.');await fs.writeFile(previous,old);
const handoffUrl=new URL('docs/HANDOFF.md',root);let handoff=await fs.readFile(handoffUrl,'utf8');
const section=`# Handoff 2026-09-17 - H04/H05 servidor aplicado; clientes locales\n\n- Continuación autorizada. Aplicada solo 20260918020000_clinical_retry_and_residual_revocation.sql tras baseline exacto y candidata en rollback. H01-H03 siguen protegidos.\n- Reintento de atención devuelve comprobante original con misma autorización, autor, clave y contenido; rechaza cambios. Documentos preparados vinculan metadata; ready es idempotente; pending exige permiso vigente. Revocar completed funciona sin borrar historial.\n- Web guarda checkpoints/archivo/claves en memoria y separa atención guardada de adjunto pendiente. Nuevo servicio clinical-encounter-submission.ts; API recupera objeto ya subido antes de subir otra vez. Mobile agrega Revocar permisos pendientes en historial completed vigente. Sin nuevos DTOs.\n- 36 SQL locales, 9 cliente/servicio, 25 remotos antes/después y 7 navegador/Storage real pasaron. Navegador simuló pérdida de respuesta tras subida: 1 atención, 1 preparación y 1 subida. Owner revocó completed y conserva historia. No prueba de dos conexiones ni QA nativo.\n- Evidencia y reproducción: docs/audit/2026-09-17/CORRECCION_REINTENTOS.md y evidence/clinical-retry-*.json. Atención sintética 9601e6ff-8318-4775-bbf5-aa51c2f5c527 y PNG retenidos. Perfil QA suspendido de nuevo, expiración restaurada y grants/consentimientos revocados. Habilitación temporal fue gestión de fixture, no flujo administrativo de rehabilitación.\n- Servidor aplicado; publicación web/mobile pendiente. No commit/push ni despliegue de clientes. Recargar página pierde checkpoint en memoria. No afirmar auditoría completa: 45/110 fichas parcialmente ejecutadas; 65 pendientes.\n- Próximo: publicar clientes y QA nativo, corregir H06 capacidad, luego H07/H08 web; ampliar cobertura funcional. Conservar app.json, docs/delivery/onlyoneaccess.txt y demás cambios previos ajenos.\n\n`;
if(!handoff.includes(section.split('\n')[0]))handoff=handoff.replace('# HANDOFF.md\n','# HANDOFF.md\n\n'+section);await fs.writeFile(handoffUrl,handoff);
